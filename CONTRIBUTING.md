# 贡献说明

感谢你帮助改进潮湿雨季。项目目前面向 Windows 本地运行、单账号女性情感随笔创作，保留人工编辑、确认和手动发布流程。

## 提交问题或建议

在 Issues 中选择“问题反馈”或“功能建议”，描述操作步骤、实际结果和期望结果。请提供 Windows、Python、Node.js 和项目版本；日志只保留能说明问题的脱敏片段。

不要上传 `.env`、API Key、Cookie、数据库或私人文章。示例内容请使用虚构数据。

## 本地开发

1. Fork 仓库并克隆，在自己的功能分支修改。
2. 按 README 安装依赖。
3. 后端和前端开发服务的启动方式见 [开发说明](docs/DEVELOPMENT.md)。
4. 修复问题时，优先添加能复现问题的回归测试。
5. 提交 Pull Request，说明问题、最终行为和验证结果。

在项目根目录运行：

```powershell
.\.venv\Scripts\python.exe -m pytest -q
```

在 `frontend` 目录运行：

```powershell
npm.cmd run lint
npm.cmd test
npm.cmd run build
```

测试使用独立数据目录，外部服务通过模拟响应验证。不要让自动测试使用真实密钥或产生付费调用；真实服务实测应单独执行并记录范围。

## 修改约定

- 保持现有界面语言和人工确认流程，错误信息应帮助用户继续操作。
- 数据库结构变更应添加迁移，并验证已有数据升级。
- 修改接口时同步前端类型、文档和相关测试。
- 不提交运行数据、依赖目录、构建缓存或个人路径。
- 新功能涉及联网或外部模型时，明确发送的数据、失败行为和费用提示。
- 避免混入无关格式化或批量依赖升级。

当前项目未附加开源许可证；提交前请核对所包含代码和资源的来源，并与维护者确认适用约定。
